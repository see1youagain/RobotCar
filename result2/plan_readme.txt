调用parse_string()实现运输顺序输出
eg：
input_str = "4:A|1:B|2:C|3:0"
parse_string(input_str) 


输出的是两个list如下：
#记录取药的顺序
window = [ ]
#记录送药到哪个区域的顺序
num = [ ]


调用init()可以实现创建线程，开始计数ABC药品的量（给你的程序里面我删了这个调用，你看你在哪个地方调用，就直接init()）

【这部分需要跟朱俊安对接，标志位怎么给】我想的是去往快递员的配药区就把标志位置1
flag 用于标志取药成功与否
 取药成功为1，否则为0
用于实时更新取药之后，药物余量


【具体代码分割，我在代码里标志了的】